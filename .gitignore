# Secrets — never commit these
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Generated at runtime — checkpoints & FAISS index are committed deliberately
# (see note below) so the deployed app works without retraining on startup.
# If you'd rather NOT commit large binaries, uncomment these two lines:
# backend/app/models/saved/*.pt
# backend/app/rag/faiss.index